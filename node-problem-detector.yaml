apiVersion: extensions/v1beta1
kind: DaemonSet
metadata:
  name: node-problem-detector
spec:
  template:
    metadata:
      labels:
        app: node-problem-detector
    spec:
      containers:
      - name: node-problem-detector
        command:
        - /node-problem-detector
        - --kernel-monitor=/config/kernel-monitor.json
        image: gcr.io/google_containers/node-problem-detector:v0.2
        imagePullPolicy: Always
        securityContext:
          privileged: true
        env:
        - name: NODE_NAME
          valueFrom:
            fieldRef:
              fieldPath: spec.nodeName
        volumeMounts:
        - name: config
          mountPath: /config
          readOnly: true
        - name: log
          mountPath: /log
          readOnly: true
        - name: kmsg
          mountPath: /dev/kmsg
          readOnly: true
      volumes:
      - name: config
        configMap:
          name: node-problem-detector-config
      - name: log
        # No longer used, but retained for backwards compatibility with old versions
        hostPath:
          path: /var/log
      - name: kmsg
        hostPath:
          path: /dev/kmsg
